Scenario,Command/Code,Output
CLI: Single mutation,python predict_mutation.py --position 8606 --base C,Formatted severity prediction with confidence
CLI: Multiple mutations (batch),for pos in 100 200 300; do python predict_mutation.py --position $pos --base C; done,Individual predictions for each mutation
Python API: Single prediction,"from predict_mutation import PredictionPipeline
pipeline = PredictionPipeline()
result = pipeline.predict(8606, ""C"")","Dictionary with prediction, confidence, probabilities"
Python API: Batch predictions,"results = [pipeline.predict(pos, base) for pos, base in mutations_list]",List of dictionaries for each variant
Generate predictions for all possible SNVs,"for pos in range(1, len(ref_seq)):
  for base in [""A"",""C"",""G"",""T""]:
    if ref_seq[pos] != base: predict(pos, base)",Genome-wide variant effect predictions
